#include "main.h"

/**
 * add - Prints the addition of two integers
 * @a: an integer
 * @b: an integer
 * Return: The addition of the two numbers
 */
int add(int a, int b)
{
	int a, b, result;

	result = a + b;

	return (result);
}
